export const getContacts = (state) => state.contacts.items;
export const getIsLoading = (state) => state.contacts.isLoading;
export const getErrors = (state) => state.contacts.error;
export const getFilter = (state) => state.filter;
